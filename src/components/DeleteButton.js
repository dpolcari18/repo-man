import React from 'react'

// react bootstrap
import Button from 'react-bootstrap/Button'

const DeleteButton = () => {
    return (
        <Button>
            Delete
        </Button>
    )
}

export default DeleteButton